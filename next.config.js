/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.teachertrainingkolkata.com', 'i.gifer.com','res.cloudinary.com'],
  },
}

module.exports = nextConfig
